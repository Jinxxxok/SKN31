# 26-05-08(금)
## 03_Pandas 시각화
### 히스토그램
tips[['tip', 'total_bill']].plot(
    kind='hist',
    alpha=1,
    bins=30,
    subplots=True,
    layout=(1,2)
    ) # subplots 여러개 axes에 나눠서 그린다 defalut = False
      # layout = (몇행, 몇열) 그리는 위치(레이아웃) 조정 가능
      # alpha = 그래프 선명도

### Boxplot 상자그래프
```
tips['total_bill'].plot(kind="box", figsize=(4, 3))
plt.show()
```
- 이상치 확인용
- 4분위가 기반 / 정규분포를 기준으로 이상치를 찾기도 가능
```
q1, q2, q3 = tips['total_bill'].quantile(q=[0.25, 0.5, 0.75])
print(q1,q2,q3)

whis = 1.5 # 범위를 조정하는 파라미터
iqr = q3 - q1 # 1 ~ 3분위의 값 iqr값
nr1 = q1 - iqr * whis # iqr값을 1분위에서 내리고
nr2 = q3 + iqr * whis # 3분위에서 뺀 정상범위 ( whis /  범위의 기본값 1.5 )
   * nr1 - nr2 사이값이 정상범위 나가면 이상치
```
### Scatter plot 산점도
- df.plot(kind='scatter', x= 'x축에 놓을 컬럼이름', y = 'y축에 놓을 컬럼 이름')
```
tips.plot(kind="scatter", x="tip", y="total_bill", alpha=0.5)
plt.show()
```
```
tips[['tip', 'total_bill']].corr()
```
- .corr() = 상관관계 보여주는 함수(상관계수)

#### 컬럼 간 상관 계수 시각화 (히트맵)
- matplotlib의 imshow()를 이용해 히트맵 그리기.
```
plt.imshow(v, cmap="Blues") # cmap => color map: 색깔별로 농도를 이용해 수를 나타내주는 히트맵
plt.colorbar()
plt.show()
```

#### 이미지 불러오기
```
from PIL import Image
import numpy as np
img = np.array(Image.open("line.png"))
plt.imshow(img)
plt.show()
```

이미지 파일 array로 전환 rgb값 dataframe에서 확인가능

### line plot
#### 계절별 강수량 차이
```
df = pd.read_excel("data/강수량.xlsx", index_col="계절").T
df['봄'].plot(figsize=(15, 4), marker="o")
plt.show()
```
- line plot이 kind 디폴트 값이기 때문에 생략해도 line plot.
- .T 행열 인덱스 전환
#### 년도별 강수량 차이
```
df.T.plot(figsize=(10,4))
plt.legend(bbox_to_anchor=(1,1), loc="upper left", title="년도", ncols=3)
plt.show()
```

## ML 01_Numpy개요_배열생성

### Numpy

ML : 과거의 dataset을 가지고 패턴을 찾아 미래 예측

#### Numpy 데이터 구조
-   <b style='font-size:1.3em'>스칼라 (Scalar)</b>
    -   값 하나. (0차원)
-   <b style='font-size:1.3em'>벡터 (Vector)</b>
    -   여러개의 값들을 순서대로 모아놓은 데이터 모음(데이터 레코드)
    -   1D Tensor, 1D Array (1차원 배열)
-   <b style='font-size:1.3em'>행렬 (Matrix)</b>
    -   벡터들을 모아놓은 데이터 집합. 2개의 방향으로 값들이 관리된다.
    -   2D Tensor, 2D Array (2차원 배열)
-   <b style='font-size:1.3em'>텐서 (Tensor)</b>
    -   같은 크기의 행렬들(텐서들)을 모아놓은 데이터 집합. N개의 방향으로 값들이 관리된다.
    -   ND Tensor, ND Array (다차원 배열)

#### 용어
-   **축 (axis)**
    -   값들의 나열 방향
    -   하나의 축(axis)는 하나의 범주(분류, Category)이다.
-   **랭크 (rank)**
    -   데이터 집합에서 축의 개수.
    -   차원 (dimension) 이라고도 한다.
-   **형태/형상 (shape)**
    -   각 축(axis) 별 데이터의 개수
-   **크기 (size)**
    -   배열내 원소의 총 개수

### 넘파이 배열 (ndarray)

- numpy에서 제공하는 자료구조 N차원 배열 객체.
- 같은 타입의 값들만 가질 수 있다.
- 축(axis)별 데이터의 개수는 모두 동일하다.
- 빠르고 메모리를 효율적 사용 / 벡터 연산과 브로드캐스팅 기능 제공.

> <b style='font-size:1.5em'>차원 (dimension)</b>
>
> -   **Vector에서 차원**: 원소의 개수
> -   **넘파이 배열에서 차원**: 축의 개수


